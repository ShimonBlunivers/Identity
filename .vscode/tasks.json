{
    "version": "2.0.0",
    "tasks": [
        {
            "type": "shell",
            "label": "Start server",
            "command": "java -agentlib:jdwp=transport=dt_socket,server=y,suspend=n,address=1000 -jar server.jar nogui",
            "options": {
                "cwd": "${workspaceFolder}/server"
            },
            "problemMatcher": []
        }
    ]
}

